<div class="center jumbotron">

  <h1>&Aacute;reas</h1>

  <%= flash_messages %>

  <table id="areas-table", data-source="<%= areas_path(format: :json)  %>" class="table table-striped table-bordered dt-responsive nowrap" width="100%" cellspacing="0">
    <thead>
      <tr>
        <th>&Aacute;rea</th>
        <th>Centro de Costos</th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
    </tbody>
  </table>

  <%= link_to '<i class="glyphicon glyphicon-plus"></i>'.html_safe, new_area_path, class: "btn btn-lg btn-primary" %>
  <%= link_to '<i class="glyphicon glyphicon-arrow-left"></i>'.html_safe, root_path, class: "btn btn-lg btn-primary" %>

</div>